# 設置和訪問

了解如何設置和訪問 Canyon 社區版。

## 系統設置

想要使用 Canyon 的所有功能，您必須配置以下數據表：

1. `CANYON_SERVER`: Canyon 服務器地址
2. `GITLAB_SERVER`: GitLab 服務器地址
3. `GITLAB_CLIENT_ID`: GitLab 客戶端 ID
4. `GITLAB_CLIENT_SECRET`: GitLab 客戶端密鑰

NOTE: 回調地址為 `<CANYON_SERVER>/oauth`，請確保在 GitLab 中配置正確。

import { Callout } from 'nextra/components';

```sql copy
insert into public.sys_setting (id, key, value)
values  ('1', 'CANYON_SERVER', 'https://canyonjs.org'),
        ('2', 'GITLAB_SERVER', 'https://gitlab.com'),
        ('3', 'GITLAB_CLIENT_ID', 'xxx'),
        ('4', 'GITLAB_CLIENT_SECRET', 'xxx');
```

<Callout type="info" emoji="ℹ️">
  [將 GitLab 配置為 OAuth 2.0 身份驗證身份提供者](https://docs.gitlab.com/ee/integration/oauth_provider.html)
</Callout>

目前只支持 GitLab，後續會支持更多的 Git 服務商。

```sql copy
insert into public.git_provider (id, url, type, name, disabled, private_token)
values  ('gitlab', 'https://gitlab.com', 'gitlab', 'GitLab', false, 'xxx');
```

<Callout type="info" emoji="ℹ️">
  [生成 GitLab Private Token](https://docs.gitlab.com/user/profile/personal_access_tokens/#create-a-personal-access-token)
</Callout>

## 管理員登錄

創建以下用戶。您可以使用以下憑據登錄： email: canyon，password: 123456

```sql copy
INSERT INTO "user" (id, email, password, nickname, avatar, favor)
VALUES ('canyon', 'canyon@canyon.com', '123456', 'Canyon', '/avatar.jpg', '');
```

## gitlab oauth2 登陸

![](https://cdn.jsdelivr.net/gh/canyon-project/assets/docs/static/docs/self-host/community-edition/login.png)
