# Canyon 上報器

Canyon 上報器是一個用於上報覆蓋率數據的二進制工具。提供 linux、macOS、windows 三個平台的二進制文件。

最新版本可前往 [GitHub Release](https://github.com/canyon-project/canyon/releases) 下載。

## 功能

讀取本地 `.canyon_output` 目錄下的覆蓋率數據聚合上報

## 使用

```bash
chmod +x ./canyon-uploader
canyon-uploader map --report-id=reportID
```
