# Lynx

import { Steps } from "nextra/components";

安裝和配置 Lynx 。

<Steps>

  ### 創建項目

  [Lynx](https://lynxjs.org/) 文檔

  ### 添加 Canyon 及其配置

  > [!TIP]
  >
  > Lynx編譯器默認使用swc。插件配置相關[文檔](https://lynxjs.org/zh/api/rspeedy/rspeedy.tools.swc.html#toolsswc-property)


  ```sh npm2yarn copy
  npm install swc-plugin-coverage-instrument swc-plugin-canyon -D
  ```

  ### 配置 swc 插件

  ```js filename="lynx.config.ts" {19,20} copy
  import { defineConfig } from '@lynx-js/rspeedy'
  import { pluginQRCode } from '@lynx-js/qrcode-rsbuild-plugin'
  import { pluginReactLynx } from '@lynx-js/react-rsbuild-plugin'

  export default defineConfig({
    plugins: [
      pluginQRCode({
        schema(url) {
          return `${url}?fullscreen=true`
        },
      }),
      pluginReactLynx(),
    ],
    tools:{
      swc:{
        jsc:{
          experimental:{
            plugins:[
                ['swc-plugin-coverage-instrument',{}],
                ['swc-plugin-canyon',{}]
            ]
          }
        }
      }
    }
  })

  ```

  ### 就是這樣
  繼續查看 [第一個覆蓋率數據 - 檢查](/docs/getting-started/first-coverage#%E6%A3%80%E6%9F%A5)。
</Steps>


