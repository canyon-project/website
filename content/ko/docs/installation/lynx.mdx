# Lynx

import { Steps } from "nextra/components";

Lynx 설치 및 구성.

<Steps>

  ### 프로젝트 생성

  [Lynx](https://lynxjs.org/) 문서

  ### Canyon 및 구성 추가

  > [!TIP]
  >
  > Lynx 컴파일러는 기본적으로 swc를 사용합니다. 플러그인 구성 관련 [문서](https://lynxjs.org/zh/api/rspeedy/rspeedy.tools.swc.html#toolsswc-property)


  ```sh npm2yarn copy
  npm install swc-plugin-coverage-instrument swc-plugin-canyon -D
  ```

  ### swc 플러그인 구성

  ```js filename="lynx.config.ts" {19,20} copy
  import { defineConfig } from '@lynx-js/rspeedy'
  import { pluginQRCode } from '@lynx-js/qrcode-rsbuild-plugin'
  import { pluginReactLynx } from '@lynx-js/react-rsbuild-plugin'

  export default defineConfig({
    plugins: [
      pluginQRCode({
        schema(url) {
          return `${url}?fullscreen=true`
        },
      }),
      pluginReactLynx(),
    ],
    tools:{
      swc:{
        jsc:{
          experimental:{
            plugins:[
                ['swc-plugin-coverage-instrument',{}],
                ['swc-plugin-canyon',{}]
            ]
          }
        }
      }
    }
  })

  ```

  ### 완료
  계속해서 [첫 번째 커버리지 데이터 - 확인](/docs/getting-started/first-coverage#확인)을 참조하세요.
</Steps>


